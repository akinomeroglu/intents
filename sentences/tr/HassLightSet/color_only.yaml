---
# HassLightSet - color_only
# slots:
# - {color}
# context_area: true

language: "tr"
data:
  - sentences:
      - "[<burada>] [<butun>] <isiklar> {color} yap"
      - "[<burada>] [<butun>] <isiklar>[<in_hali>] rengini {color} yap"
    example: "ışıkları kırmızı yap"
    inferred_domain: "light"
    response: "color"
