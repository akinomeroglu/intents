---
# HassIncreaseTimer - minutes_start_seconds
# slots:
# - {start_seconds}
# - {minutes}

language: "tr"
data:
  - sentences:
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici_ya> {timer_minutes:minutes} dakika ekle"
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici_i> {timer_minutes:minutes} dakika <timer_uzat>"
    example: "30 saniyelik zamanlayıcıya 5 dakika ekle"
    response: "default"
