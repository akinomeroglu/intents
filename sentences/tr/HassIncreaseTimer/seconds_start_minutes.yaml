---
# HassIncreaseTimer - seconds_start_minutes
# slots:
# - {start_minutes}
# - {seconds}

language: "tr"
data:
  - sentences:
      - "{timer_minutes:start_minutes} dakika[lık] <zamanlayici_ya> {timer_seconds:seconds} saniye ekle"
      - "{timer_minutes:start_minutes} dakika[lık] <zamanlayici_i> {timer_seconds:seconds} saniye <timer_uzat>"
    example: "5 dakikalık zamanlayıcıya 30 saniye ekle"
    response: "default"
