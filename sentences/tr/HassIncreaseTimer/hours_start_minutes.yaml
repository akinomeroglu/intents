---
# HassIncreaseTimer - hours_start_minutes
# slots:
# - {start_minutes}
# - {hours}

language: "tr"
data:
  - sentences:
      - "{timer_minutes:start_minutes} dakika[lık] <zamanlayici_ya> {timer_hours:hours} saat ekle"
      - "{timer_minutes:start_minutes} dakika[lık] <zamanlayici_i> {timer_hours:hours} saat <timer_uzat>"
    example: "5 dakikalık zamanlayıcıya 1 saat ekle"
    response: "default"
