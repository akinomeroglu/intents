---
# HassIncreaseTimer - minutes_start_minutes
# slots:
# - {start_minutes}
# - {minutes}

language: "tr"
data:
  - sentences:
      - "{timer_minutes:start_minutes} dakika[lık] <zamanlayici_ya> {timer_minutes:minutes} dakika ekle"
      - "{timer_minutes:start_minutes} dakika[lık] <zamanlayici_i> {timer_minutes:minutes} dakika <timer_uzat>"
    example: "5 dakikalık zamanlayıcıya 5 dakika ekle"
    response: "default"
