---
# HassIncreaseTimer - hours_start_seconds
# slots:
# - {start_seconds}
# - {hours}

language: "tr"
data:
  - sentences:
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici_ya> {timer_hours:hours} saat ekle"
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici_i> {timer_hours:hours} saat <timer_uzat>"
    example: "30 saniyelik zamanlayıcıya 1 saat ekle"
    response: "default"
