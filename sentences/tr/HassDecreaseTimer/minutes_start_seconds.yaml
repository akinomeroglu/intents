---
# HassDecreaseTimer - minutes_start_seconds
# slots:
# - {start_seconds}
# - {minutes}

language: "tr"
data:
  - sentences:
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici_dan> {timer_minutes:minutes} dakika çıkar"
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici_i> {timer_minutes:minutes} dakika <timer_kisalt>"
    example: "30 saniyelik zamanlayıcıdan 5 dakika çıkar"
    response: "default"
