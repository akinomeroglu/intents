---
# HassTimerStatus - seconds_only
# slots: {start_seconds}

language: "tr"
data:
  - sentences:
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici_da> ne kadar (kaldı|var)"
      - "{timer_seconds:start_seconds} saniye[lik] <zamanlayici> durumu [nedir]"
    example: "30 saniyelik zamanlayıcıda ne kadar kaldı"
    response: "default"
